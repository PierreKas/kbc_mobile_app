import 'package:flutter/material.dart';

class Anouncements extends StatelessWidget {
  const Anouncements({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Anouncement');
  }
}
