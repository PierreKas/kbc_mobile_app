import 'package:flutter/material.dart';

class Offerings extends StatelessWidget {
  const Offerings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Offerings');
  }
}
